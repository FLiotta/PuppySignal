// @Packages
import ApiService from '../../services/ApiService';

interface IProps { };

const Auth: React.FC<IProps> = (props) => {
  return (
    <div>
      <p>auth</p>
    </div>
  );
}

export default Auth;