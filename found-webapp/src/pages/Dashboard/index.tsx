// @Packages

// @Project
import './styles.scss';

interface IProps { }

const Dashboard: React.FC<IProps> = (props) => {
  return (
    <div className="dashboard">
      <p>Dashboard</p>
      <small>It seems there's nothing here yet =(</small>
    </div>
  );
};

export default Dashboard;